# ─── 비밀 (절대 커밋 금지) ────────────────────────────────
keys.json
*.key
*.pem
secrets.json
.env
.env.*
.dev.vars

# ─── 런타임 데이터 (앱이 생성) ────────────────────────────
.data/
*.db
*.db-journal
*.sqlite
*.sqlite3
pc_id

# ─── 대용량 외부 자산 (앱이 자동 내려받음) ────────────────
.data/bin/
.data/kronos/
*.exe
!installer/*.exe

# ─── 빌드 산출물 ──────────────────────────────────────────
build/
dist/
# version.py 에서 생성되는 윈도우 버전 리소스
version_info.txt
*.spec.bak
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ─── 생성 리포트 / 캐시 ───────────────────────────────────
report_out/
webapp/reports/*
!webapp/reports/.gitkeep
cache/
*.parquet
*.pkl

# ─── 노드 ─────────────────────────────────────────────────
node_modules/
.wrangler/

# ─── OS / 에디터 ──────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# 작업용 임시 스크립트 (_ 로 시작)
_*.py

# eDEX-UI 자산 — tools/fetch_edex_assets.py 로 받는다.
# 저장소에 커밋하지 않는 편이 낫다고 판단하면 아래 주석을 해제할 것.
# webapp/static/audio/
