# Plutus — 개발·테스트용
# =======================
#   pip install -r requirements-dev.txt
#
# 실행에 필요한 것은 requirements.txt 에 있다. 여기는 그 위에 얹는다.

-r requirements.txt

pytest>=8.0
pytest-timeout>=2.2      # 네트워크를 타는 테스트가 매달리지 않게

# EXE 빌드
pyinstaller>=6.0

# tools/make_icons.py 전용 — 앱 실행에는 필요 없다.
# (EXE 에서는 일부러 제외한다. 보고서 차트는 인라인 SVG 라 그리기
#  라이브러리가 아예 필요 없고, 넣으면 11MB 가 늘어난다.)
pillow>=10.0
