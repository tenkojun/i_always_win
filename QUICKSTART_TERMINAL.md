# I ALWAYS WIN — 실행 안내

eDEX-UI 풍 사이파이 터미널 + 토스 편의성을 결합한 금융 대시보드.
기존 `engine_kr` 분석 엔진이 "종목 분석 / 포트폴리오" 두뇌로 들어가 있습니다.

---

## 1. PC에서 바로 실행 (가장 빠름)

```bash
pip install -r requirements.txt
python run_desktop.py
```

- 자동으로 브라우저(또는 앱 창)가 열립니다.
- 콘솔에 표시되는 `폰 : http://<IP>:8765` 주소를 휴대폰 브라우저에
  입력하면 **같은 와이파이에서 폰으로도** 사용 가능합니다.
- `pip install pywebview` 를 추가하면 브라우저가 아닌 **독립 앱 창**으로 뜹니다.

서버만 따로 띄우려면:

```bash
python -m webapp.server
```

---

## 2. Windows .exe 로 배포

윈도우 PC에서 (윈도우 exe는 반드시 윈도우에서 빌드):

```
build_windows_exe.bat   ← 더블클릭
```

또는 수동으로:

```bash
pip install -r requirements.txt pyinstaller pywebview
pyinstaller app.spec --noconfirm
```

결과물: `dist/QuantTerminal/QuantTerminal.exe`
이 폴더를 통째로 복사해 배포하면 됩니다. exe 더블클릭 →
대시보드가 열리고, 같은 와이파이의 폰에서도 접속 가능합니다.

> 맥은 같은 명령으로 `.app`, 리눅스는 실행 파일이 생성됩니다.

---

## 3. 화면 구성

| 영역 | 내용 |
|------|------|
| 상단 바 | 시계 · LIVE/오프라인 배지 |
| 시장 개요 | 코스피·코스닥·S&P500·나스닥·다우·원/달러·VIX·금·WTI·비트코인 (20초마다 갱신) |
| 차트 | 종목 검색(한글 별칭 지원) + 캔들 차트 + 기간(5D~5Y) |
| 종목 분석 / 포트폴리오 | 엔진 실행 → 기관 스코어카드(등급/의견) · 단중장 시그널 · 모듈별 한글 분석 · 전체 HTML 리포트 |
| 실시간 속보 | 야후 종목 뉴스 + 파이낸스 RSS (60초마다 갱신) |

---

## 4. 한계 (정직하게)

- **데이터 지연**: 야후 무료 시세는 실시간(틱)이 아니라 **약 15분 지연**입니다.
  무료로 쓸 수 있는 가장 현실적인 소스라 사용하며, 배지에 `LIVE 15M`으로 표기됩니다.
- **인터넷 필요**: 야후 데이터·차트 모듈·뉴스는 인터넷 연결이 있어야 합니다.
  연결이 없으면 합성 데이터로 화면은 뜨지만 "오프라인(데모)"로 표시됩니다.
- **분석 소요 시간**: 종목 분석은 ML·몬테카를로를 포함해 30초~2분 걸립니다.
- **뉴스 범위**: 유료 속보 API 없이 야후 뉴스/RSS 기반입니다.
- 모든 수치는 확률적 추정이며 투자 권유가 아닙니다.
