# Plutus — 실행에 필요한 것 전부, 그리고 그것만
# ==============================================
# 여기 있는 패키지는 **실제로 import 되는 것만** 남긴다.
# 목록이 실제와 어긋나면 새로 받은 사람이 문서대로 해도 실패한다.
#
#   pip install -r requirements.txt
#
# 개발/테스트까지 하려면:  pip install -r requirements-dev.txt

# ─── 수치 · 통계 ─────────────────────────────────────────────
# jiqtx 엔진이 GJR-GARCH-t MLE · HAR-RV · Jump Model 을 이 넷으로
# **직접 구현**한다. arch / statsmodels / hmmlearn 을 쓰지 않는다.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# ─── 데이터 ─────────────────────────────────────────────────
yfinance>=0.2.30
requests>=2.31
websockets>=12.0        # KIS 실시간 시세 (webapp/server.py)

# ─── 웹 · 데스크톱 ──────────────────────────────────────────
flask>=3.0
# Flask 가 함께 설치하지만 우리가 **직접** import 한다(HTTPException).
# 직접 쓰는 것은 명시한다 — 남의 의존성에 얹혀 가면 그쪽이 바뀔 때 깨진다.
werkzeug>=3.0
pywebview>=4.4          # 윈도우에서는 Edge WebView2 런타임이 따로 필요하다
psutil>=5.9

# ─── 빌드 (EXE 를 구울 때만) ────────────────────────────────
# pyinstaller>=6.0

# ─── 여기 없는 이유 ─────────────────────────────────────────
# matplotlib · PIL   보고서 차트는 인라인 SVG 를 문자열로 만든다.
#                    그리기 라이브러리가 아예 필요 없다.
# arch · statsmodels · hmmlearn
#                    변동성/레짐 모델을 scipy 로 직접 구현했다.
#                    게다가 이 둘은 윈도우에서 컴파일러 없이 설치가
#                    실패하는 일이 잦아, 목록에 있으면 첫 설치부터 막힌다.
# torch · xgboost    v2.15.0 에서 구엔진과 함께 제거됐다.
#
# 위 여섯은 import 하는 파일이 0개다. 새로 넣지 말 것.
