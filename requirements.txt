# 핵심
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3

# 데이터
yfinance>=0.2.30

# ML / DL
xgboost>=1.7
torch>=2.0
hmmlearn>=0.3
shap>=0.43
# Kronos foundation model (K-line forecasting) — optional
transformers>=4.40
huggingface_hub>=0.20
einops>=0.7

# 통계 / 변동성
statsmodels>=0.14
arch>=6.2

# 유틸
tqdm>=4.65
jinja2>=3.1
flask>=3.0
requests>=2.31
psutil>=5.9
websockets>=12.0       # KIS 실시간 WebSocket (틱/호가)

# 백테스트
vectorbt>=0.27
numba>=0.59
llvmlite>=0.42       # numba 의존성 (PyInstaller 명시)

# C12 SSE 안전망 — stdlib만 사용하지만 wheel 누락 방지
# (queue/threading/urllib는 stdlib이므로 추가 불요)

