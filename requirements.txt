# ─── 핵심 ───────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3

# ─── 데이터 ─────────────────────────────────────────────────
yfinance>=0.2.30
pyarrow>=15.0          # 가격 캐시 (parquet)
requests>=2.31

# ─── 통계 / 변동성 / 레짐 ───────────────────────────────────
statsmodels>=0.14
arch>=6.2
hmmlearn>=0.3

# ─── ML (분석 파이프라인 내부용) ────────────────────────────
xgboost>=1.7
torch>=2.0

# ─── 웹 / 데스크톱 ──────────────────────────────────────────
flask>=3.0
jinja2>=3.1
pywebview>=5.0         # 네이티브 창 (콘솔 없는 EXE)
psutil>=5.9
tqdm>=4.65

# ─── 실시간 시세 (선택) ─────────────────────────────────────
websockets>=12.0       # KIS 실시간 WebSocket

# 제거됨: vectorbt / numba / llvmlite (전략 백테스트)
#         shap (리서치)  ·  transformers / huggingface_hub / einops (Kronos)
